╔════════════════════════════════════════════════════════════════╗
║   UPDATE YOUR VM WITH LATEST CODE                              ║
╚════════════════════════════════════════════════════════════════╝

Your VM IP: 140.245.116.86

═══════════════════════════════════════════════════════════════════

QUICK UPDATE (Copy and paste these commands)
───────────────────────────────────────────────────────────────────

SSH to your VM, then run:

cd ~/f1-telemetry-poc
git pull
pm2 restart f1-telemetry

═══════════════════════════════════════════════════════════════════

WHAT WAS FIXED
───────────────────────────────────────────────────────────────────
✅ Fixed UTF-8 encoding (no more weird characters!)
✅ Beautiful new UI design
✅ Clear status indicators:
   - 🟢 Green = Connected and receiving data
   - 🟠 Orange = Waiting for F1 25
   - 🔴 Red = Error/Disconnected
✅ Shows packets received counter
✅ Shows last packet time
✅ Better instructions
✅ Server info panel
✅ WebSocket status

═══════════════════════════════════════════════════════════════════

STEP-BY-STEP UPDATE INSTRUCTIONS
───────────────────────────────────────────────────────────────────

1. SSH to your VM:

   ssh -i C:\NoSync\F1\f1telemetry.key opc@140.245.116.86

2. Go to project directory:

   cd ~/f1-telemetry-poc

3. Pull latest code from GitHub:

   git pull

4. Restart server:

   pm2 restart f1-telemetry

5. Check status:

   pm2 status

6. View logs (optional):

   pm2 logs

7. Refresh browser:

   http://140.245.116.86:3000

═══════════════════════════════════════════════════════════════════

VERIFY IT'S WORKING
───────────────────────────────────────────────────────────────────

✅ Dashboard loads with no weird characters
✅ Status shows "Waiting for F1 25 connection..."
✅ Shows your VM IP address
✅ WebSocket status shows "Connected"
✅ UI looks clean and professional

═══════════════════════════════════════════════════════════════════

TROUBLESHOOTING
───────────────────────────────────────────────────────────────────

Problem: git pull fails
Solution: cd ~/f1-telemetry-poc
          git reset --hard
          git pull

Problem: Server won't restart
Solution: pm2 delete f1-telemetry
          pm2 start server-simple.js --name f1-telemetry
          pm2 save

Problem: Still seeing old UI
Solution: Hard refresh browser (Ctrl+Shift+R or Ctrl+F5)

═══════════════════════════════════════════════════════════════════

READY TO TEST!

Dashboard: http://140.245.116.86:3000

